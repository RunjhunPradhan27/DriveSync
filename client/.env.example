# Base URL for the DriveSync API. Leave as /api to use the Vite dev-server
# proxy (see vite.config.js), which forwards requests to the backend with no
# CORS configuration required.
VITE_API_BASE_URL=/api
